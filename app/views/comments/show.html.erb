<% provide(:title, 'View Comment') %>
<div class="panel panel-default">
  <div class="panel-body">
<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @comment.commenter %>
</p>

<p>
  <%= @comment.body %>
</p>
<%= link_to 'Edit', edit_post_comment_path(@comment.post, @comment) %> |
<%= link_to 'Back', post_comments_path(@comment.post) %>
  </div>
</div>